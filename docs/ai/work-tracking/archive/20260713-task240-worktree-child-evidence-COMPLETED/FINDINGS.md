# Findings

- 2026-07-13 — The Git-common-dir keyed store, concurrent SQLite writes, and teardown preservation already pass; replacing the store would add migration risk without fixing the observed attribution gap.
- 2026-07-13 — Codex 0.144 reports hooks as stable and documents PostToolUse plus SubagentStart/SubagentStop. PostToolUse includes failures, but its documented tool payload does not promise a child agent id; lifecycle identity and worktree ownership can be recorded without fabricating tool-level child attribution.
- 2026-07-13 — The source checkout has an untracked local `.codex/hooks.json`. Task 240 must prove merged installer behavior in disposable targets and leave that local drift untouched.
- 2026-07-13 — The first concurrent installed-target run exposed a sixth row: automatic scope inference. Its repository context was correct but it initially dropped child/parent identity; propagating the triggering hook identity closed that provenance gap rather than weakening the acceptance count.
- 2026-07-13 — Same-second lifecycle timestamps cannot establish begin/end ordering when secondary sorting uses random event IDs. SQLite `seq` is the authoritative monotonic order for the shared ledger and deterministically preserves `subagent_begin` before `subagent_end`.
- 2026-07-13 — Codex project command hooks are synchronous in the current client even when an async field is parsed. The managed commands must therefore stay fast, failure-proof, and synchronous until the client implements async command hooks.
- 2026-07-13 — Root-session rows correctly have no parent. Every child-derived installed-scenario row carries `session:parent-session`; deeper ancestry remains factual only when supplied by the client or `AEGIS_PARENT_AGENT_ID` bridge.
- 2026-07-13 — The source implementation worktree intentionally has no installed-target manifest/shim. Source-CLI strict verify must refuse there; installing duplicate workflow state would violate the task contract. Source guard, Taskmaster, full tests, scanners, and hosted witness are the applicable lifecycle gates.
- 2026-07-13 — One unchanged reconcile safety test assumes the governed checkout is outside the system temp directory. Because this isolated worktree lives under `/tmp`, the test correctly reaches a later fail-closed reason locally; its unchanged normal-path assertion is deferred to hosted CI.
