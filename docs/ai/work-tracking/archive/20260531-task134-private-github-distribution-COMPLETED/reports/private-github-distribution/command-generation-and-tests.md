# Task 134 Command Generation And Focused Test Evidence

## Command Generation

Claude command generated by:

```bash
python3 scripts/codex-task aegis mcp generate-registration --client claude --scope user --source-mode private-github --github-ref main
```

Rendered command:

```bash
claude mcp add --scope user aegis -e UV_CACHE_DIR=.aegis/uv-cache -e UV_TOOL_DIR=.aegis/uv-tools -- uvx --from git+ssh://git@github.com/loucmane/codex-starter-pack.git@main aegis-mcp-server --default-target-dir . --transport stdio
```

Codex command generated by:

```bash
python3 scripts/codex-task aegis mcp generate-registration --client codex --source-mode private-github --github-ref main
```

Rendered command:

```bash
codex mcp add --env UV_CACHE_DIR=.aegis/uv-cache --env UV_TOOL_DIR=.aegis/uv-tools aegis -- uvx --from git+ssh://git@github.com/loucmane/codex-starter-pack.git@main aegis-mcp-server --default-target-dir . --transport stdio
```

Both payloads reported:

- `source_mode`: `private-github`
- `package_spec`: `git+ssh://git@github.com/loucmane/codex-starter-pack.git@main`
- safety note: `private-github-requires-native-git-auth`

## Focused Tests

Command:

```bash
env UV_CACHE_DIR=/tmp/uv-cache-task134 PYTHONDONTWRITEBYTECODE=1 uv run python -m pytest tests/meta_workflow_guard/test_aegis_native_mcp_registration.py tests/meta_workflow_guard/test_aegis_invocation_contract.py -q
```

Result:

```text
33 passed in 16.40s
```

The first run without `UV_CACHE_DIR=/tmp/uv-cache-task134` failed because sandboxed uv attempted to open `/home/loucmane/.cache/uv/sdists-v9/.git` on a read-only filesystem. The rerun used the writable `/tmp` cache and passed.

## Broader Aegis Regression Suite

Command:

```bash
env UV_CACHE_DIR=/tmp/uv-cache-task134 PYTHONDONTWRITEBYTECODE=1 uv run python -m pytest tests/meta_workflow_guard/test_aegis_mcp_server.py tests/meta_workflow_guard/test_aegis_schemas.py tests/meta_workflow_guard/test_aegis_installer.py tests/meta_workflow_guard/test_aegis_native_mcp_registration.py tests/meta_workflow_guard/test_aegis_invocation_contract.py -q
```

Result:

```text
138 passed, 1 skipped in 26.81s
```

The skipped test is the opt-in full release certification smoke guarded by `AEGIS_RUN_CERTIFICATION_SMOKE=1`.
