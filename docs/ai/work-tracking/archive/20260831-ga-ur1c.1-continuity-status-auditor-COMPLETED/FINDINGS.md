# Findings

- 2026-08-31 — The project registry contains four canonical roots, but `gas-city` and `gas-city-operations` intentionally share the `gascity` rig. Treating every project as a separate ledger duplicated the same Beads and produced contradictory next-action output. The model now captures a rig once and keeps projects as evidence/coverage surfaces.
- 2026-08-31 — Without initiative scoping, every open historical HPFetcher Bead became a global next action. The authoritative active plan is now the open `initiative:active` epic plus transitive `parent-child` descendants. Other ledgers remain visible for parity and orphan detection without silently joining the plan.
- 2026-08-31 — The installed Obsidian registry targets `/home/loucmane/gas-city-ops` but retains registry project ID `gas-city`; its last no-change cycle records live index status `not-run-no-change`. Both are now explicit errors instead of being mistaken for a current Operations projection.
- 2026-08-31 — Live capture found eight unbound managed Git surfaces: four branches and four worktrees across Gas City and Operations. These are preserved findings for terminality/supersession reconciliation; the auditor performs no cleanup.
- 2026-08-31 — HPFetcher retains one historical Taskmaster ACTIVE folder. It is classified as a warning under Legacy and no native Bead is fabricated.
- 2026-08-31 — `ga-ur1c.1` briefly read as open after the preserved lifecycle recovery. Explicit reclaim restored `in_progress`/`loucmane`; subsequent `list` and `show` observations agree.
- 2026-08-31 — The machine-generated `resume --root <worktree>` command failed when the original begin used an explicit slug different from the title-derived slug. Resume now recovers the immutable slug from the existing transaction journal, eliminating the memory-only workaround.
- 2026-08-31 — The first snapshot serialized complete Bead payloads and duplicated the shared `gascity` store, producing 75,230 lines and risking publication of cross-project notes. The final contract stores each rig once, projects only classification fields, and keeps the raw snapshot out of the public source tree while committing its digest-bound report.
- 2026-08-31 — Archive preconditions were satisfied and the completed bundle was preserved.
