# Decisions

- 2026-05-28 — Chose `aegis handoff repair` plus MCP `aegis.handoff_repair` instead of weakening closeout gates.
- 2026-05-28 — Chose to preserve `## Progress Log` exactly and replace only the semantic handoff prefix, because progress entries are the audit trail.
- 2026-05-28 — Chose apply-style MCP behavior: default preview is read-only; `apply=true` is required for mutation.
