# Bead ga-vqm2 Managed-worker recovery, drift, upgrades, and reboot survival – Implementation Notes

## Workstreams

- **Blog access — complete:** `blog-6r1b` merged and live-proved the narrow linked-worktree Git metadata grant. The managed worker can stage and write a tree without access to unrelated repositories.
- **Watch officer — complete:** `ga-bzn3` installed the stable named-session prompt and proved immediate inbox-first, exactly-once delivery with duplicate suppression and zero residue.
- **Session/mail recovery — complete:** `ga-9qvq` adds a core messaging/work-assignment boundary so closing a failed session cannot clear unread named-mailbox addressing. Focused tests, the full `cmd/gc` suite, vet, commit hooks, push gates, and hosted CI passed. PR #33 merged exact tree `13d09982b59f9093281a8da6d00ab51e0a2d85ea`; the Bead is closed PASS and its terminal Obsidian note is live-readable.
- **Residue recovery — complete:** the known childless, sessionless watch-officer tmux server was identity-pinned and gracefully stopped without a signal. Host authorities remained stable.
- **Recovery/reboot documentation — implemented:** `docs/operations/gas-city-managed-worker-recovery.md` consolidates observability, retries, tmux detection, signing, fixed-broker upgrades, reboot proof, and Obsidian closeout.
- **Future-project onboarding — complete:** `ga-0szv` passed its attended broker self-upgrade, exact sequence-2 fixed-operation request/receipt, stable broker and signer epochs, and byte-identical isolated synthetic render with a fourth-project overlay. It is closed PASS and its terminal Obsidian projection is live-readable.
- **Final certification — complete:** host readiness returned 19 PASS, zero FAIL/UNKNOWN, and one accepted Codex Desktop warning; all four non-HQ rigs are suspended; ledger sessions and `tmux -L city` residue are zero; the persistent Obsidian reconciler is fresh and live-readable.
