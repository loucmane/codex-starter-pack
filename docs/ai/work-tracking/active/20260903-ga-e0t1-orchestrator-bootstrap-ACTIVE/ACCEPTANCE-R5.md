# ga-e0t1 R5 — delivered and live acceptance proven

## Outcome

Source, hosted delivery, canonical activation, and the eight-case fresh-client
acceptance matrix passed on 2026-09-04. This proves the scoped Operations
orchestrator boundary, not an implementation-worker capability profile.
The parent remains open for evidence-preserving source closeout.

## Delivery and activation

- Fable independently reported 2,528 passed / 4 skipped. The executor's complete
  suite passed 2,511 / 21 skipped, the same 2,532-test corpus; no failures.
- PR #375: exact signed head `ed5bdca9ff27efdb4f2e618e55ae1f89dd43a27b`,
  tree `97ee511d96b31232241bf508c1de6ce7a51c7a8d`.
- Hosted CI run `33854888296` passed Python 3.11–3.14 and the applicable gates.
  Legacy Taskmaster compatibility was legitimately skipped by its unchanged
  classifier, not treated as a passed test.
- Repository-owned delivery run `33855270994` merged the reviewed candidate as
  `3641fa7aa11c9713da3dac3c075f50caee547ed8`. Its parents are verified base
  `32106b4017d0a7767f034295c2c302a6e08de495` and the exact candidate above;
  the resulting tree is byte-identical and GitHub's signature is valid.
- The local merge controller had independently passed head/tree/signature,
  CLEAN/MERGEABLE, green checks and zero-thread checks. It refused before any
  merge request because the remote base had already advanced. Read-only
  reconciliation proved the repository automation had performed that exact
  authorized merge; no second merge was attempted.
- Canonical main was fast-forwarded to the exact merge. The real hook loader
  resolves canonical `pretool.py` SHA-256
  `248a8257cf9dc0c51722f4aeb4d533fb20032fe59003c2a613d81d6fbc680847`
  and `permission_modes.py` SHA-256
  `c8833d37b808d48e120032d3ab315e6e3b1ae3d8efd289f754e22350ab2c860d`.

## Fresh-client acceptance

Eight separate non-persistent clients used the real merged gate and the frozen
synthetic acceptance package. Results were derived from actual tool requests,
results, disk state and approval-ledger entries, not model prose alone.

| Case | Observed result |
| --- | --- |
| Plan-mode bootstrap | Explicit hard refusal; no branch, worktree, ownership journal or Bead change. |
| Native deny precedence | Exact native deny prevents the otherwise profile-approved read. |
| Native ask precedence | Exact native ask refuses execution in the unattended client. |
| Pre-kickoff edit | Refused despite an exact native permission for the synthetic marker. |
| Normal-mode kickoff | One real supported begin creates the exact task worktree and reaches READY. |
| Task-file edit | Exactly the permitted marker changes from BEFORE to AFTER, followed by its supported evidence log. |
| Compound command | Read-plus-write composition is refused; its sentinel never exists. |
| Observation | Supported start succeeds, marker mutation is explicitly refused, and supported stop completes. |

Synthetic child: `ga-e0t1.2`. Its fixture seed is
`b81f7a09baf87e88ec3ae8a27eca7b4495d3446c`; all exported workflow source
trees match the merge. The separate observation fixture was initialized using
the supported merged installer, with no global settings or package installation.

The generated scope checklist initially caused normal workflow verification to
refuse before task editing. The executor recorded the already-reviewed synthetic
scope through `aegis log`, then the supported verification passed. This ordinary
prerequisite and the initial refusal are preserved; no plan or journal was
hand-edited and no readiness check was bypassed.

Print mode ignored untrusted project allow entries. Only the exact per-session
file/log/observation permissions declared in the package were supplied. This is
not evidence that an arbitrary interactive workspace is trusted. Earlier R4
fresh and interactive context/Bead-read acceptance remains separately preserved.
Plan mode also explicitly refuses override minting: leave plan mode before a
separately authorized recovery. Audit failure cannot turn refusal into approval;
it does not guarantee that an unavailable audit sink records an entry.

## Non-interference and evidence

- All eight owned client PIDs disappeared naturally after completion.
- At the final observation: supervisor PID `1769`, start monotonic `210566291`,
  `NRestarts=0`, active/running; all four project rigs suspended; 0/17 agents;
  no city tmux server. No rig or service transition occurred.
- User/project Claude settings, MCP settings, Codex configuration and city.toml
  retain their exact before digests, modes and owners.
- Failed R4 child `ga-e0t1.1`, its ownership, journal, fixture and failure evidence
  are unchanged. It is not counted as a successful test.
- No HPFetcher/Blog data, worker report, source work or lifecycle was involved.

Aggregate: `reports/r5-live-acceptance-audit.json`, SHA-256
`518a3e4c7ecf906bfa845e664ea5d39e4187a7af25dfedf91b1b5cd5e15aff8b`.
It binds each case's stream, result, ledger delta and baseline. Package SHA-256:
`37dc07187716b0871ced53d7a536ef4265f000261f00f03d236128d29201ee36`.
Activation and delivery: `reports/r5-activation-result.json`,
`reports/r5-pr-375-auto-reconciliation.json`, and
`reports/r5-pr-375-merge-evidence.json`. Raw reports remain local and preserved.

## Closeout hold — do not relocate the fixtures

Read-only inspection found `_execute_source_closeout` in `scripts/codex-task`
uses `os.replace(active, archive)` for the entire task folder. This folder's
ignored `reports/r4-remaining-live` and `reports/r5-live` contain real fixture
repositories and linked worktrees with frozen absolute paths. Moving the parent
folder would relocate them, invalidating those path bindings and potentially
breaking Git worktree registration. The legacy archive path also moves the
whole folder. Neither archive nor finish apply was run.

Open, unassigned follow-up `ga-fc6p` must establish a supported, regression-tested archive/preservation
contract before parent closeout; it must not move, rewrite, delete, reset or
repair the frozen fixtures. Do not improvise with symlinks, pointer edits or
manual journal changes. Successful R5 acceptance needs no replay. Keep the parent
open until the supported closeout and terminal evidence publication are proven.

Acceptance was appended exactly once to `ga-e0t1` and `ga-e0t1.2`; neither was
closed. The first recording assertion compared the child's hydrated dependency
payload against its parent's old notes, after intentionally appending those
parent notes. Read-only reconciliation proved all own fields and dependency
identities/statuses unchanged, both intended appends present exactly once, and
the failed child's own notes and update time untouched. No write was retried.
See `reports/r5-ledger-acceptance/reconciliation.json` and preserved before/after
readbacks. The follow-up was recorded without starting a parallel source context.

## Next capability work

After this closeout, resume the already-reviewed interchangeable-worker plan:
canonical Native registration, separately evidenced Claude worker provisioning,
then deterministic profile handover and its bounded cross-provider canary.
This orchestrator acceptance does not authorize or prove those later actions.
