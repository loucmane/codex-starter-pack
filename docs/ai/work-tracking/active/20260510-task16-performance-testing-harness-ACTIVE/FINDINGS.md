# Findings

- 2026-05-10 — Task 16's original wording is stale in shape but still useful in intent. The repo already has template metrics, static monitoring, Phase 0 validation, scanner profiling, and Task 1 one-off performance notes, but it lacks a reusable performance harness that times foundation operations and compares them with thresholds/baselines.
- 2026-05-10 — A quick registry discovery probe measured approximately `0.029s` for `TemplateRegistry().records()` on this repo, so the historical `<50ms` discovery target is plausible but should be report-driven and policy-driven rather than a brittle hardcoded benchmark assertion.
- 2026-05-10 — `python3 scripts/codex-guard validate --include-untracked` currently fails because Task 16 scope scaffolding was incomplete (`plan-step-scope` evidence and Serena memory), not because guard runtime is slow. This is expected during the scope gate and should clear after scope docs, memory, and plan sync are current.
- 2026-05-10 — `scripts/template-metrics-dashboard --report-dir /tmp/...` crashes while printing paths outside `REPO_ROOT` because it calls `relative_to(REPO_ROOT)` for display. This is adjacent evidence for careful path-display handling in the performance harness.
- 2026-05-10 — First strict performance harness run succeeded with warning status: cold registry record discovery exceeded the initial `0.05s` warning threshold, while warm-cache lookup, guard validation, and scanner no-checkpoint runtime all passed. This justified separating cold-index and warm-lookup thresholds.
- 2026-05-10 — Full pytest mutates the tracked legacy scanner artifact `output/data/template_scan_results.json`. The Task 16 run restored that file from HEAD after capturing full-test evidence so generated scanner runtime output stays out of this task's commit.
