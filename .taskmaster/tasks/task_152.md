# Task ID: 152

**Title:** Add CI Sacrificial Cascade Validation for Reconcile Shadow Apply

**Status:** done

**Dependencies:** 151 ✓

**Priority:** medium

**Description:** Provision deterministic Taskmaster CLI validation in GitHub Actions and run the Task 151 reconcile shadow sacrificial cascade tests in CI as pre-write evidence. Bind the validated Taskmaster version/source and relevant runner/toolchain identity to future apply evidence, preserve local skip behavior when `task-master` is unavailable, and require any Task 153+ apply path to invalidate stale cascade evidence and re-validate before apply if the pinned Taskmaster or relevant CI/toolchain identity changes.

**Details:**

Build on the existing Task 151 shadow-only implementation in `aegis_foundation/reconcile_shadow_apply.py`, especially `build_ci_shadow_context_proof`, `build_ci_shadow_cascade_validation_report`, `build_shadow_report`, `build_shadow_record`, `_predicted_paths`, and `validate_sacrificial_taskmaster_done_cascade`. Align with the current Task 152 implementation pattern in `aegis_foundation/taskmaster_toolchain.py`, where `TASKMASTER_PACKAGE_VERSION` is pinned to `0.43.1`, `taskmaster_install_spec()` returns the shared install spec, and `compare_taskmaster_toolchain_evidence()` defines the reusable stale-evidence comparison contract. Update `.github/workflows/ci.yml` to provision the deterministic `task-master` CLI before pytest in the Python matrix, using the shared `aegis_foundation.taskmaster_toolchain` lock/provisioning surface rather than embedding an ad hoc independent version in the workflow. The same lockfile/provisioning helper must be the source Task 153+ apply code uses later; future apply code must not introduce a separately pinned Taskmaster version/source. Record the pinned `task-master` version, install source, lockfile/provisioning identifier, and relevant runner/toolchain identity in `reports/ci/`, including at minimum GitHub Actions runner identity, Python matrix version, Node/npm or npx identity if used to install `task-master-ai`, OS image details available from the runner, workflow/run identity from `build_ci_shadow_context_proof`, and any package lock or resolved package metadata needed to prove the CLI provenance. Add a toolchain-binding payload to the shadow apply CI evidence so Task 153+ can compare the apply-time toolchain against the validated cascade evidence. Any mismatch in Taskmaster version/source, lockfile/provisioning identity, or relevant runner/toolchain identity must invalidate prior cascade evidence and require fresh CI sacrificial cascade validation before apply can proceed. Keep the existing `_require_taskmaster_cli()` skip guard in `tests/meta_workflow_guard/test_aegis_reconcile_shadow_apply.py` for local environments without `task-master`, but ensure GitHub Actions provisions the pinned CLI so the real-cascade tests execute there instead of skipping. Add or extend CI artifact generation beyond the current `reconcile-shadow-context-proof.json` so uploaded artifacts include: `task_master_version`, `task_master_source`, `task_master_provisioning_lock`, relevant runner/toolchain identity, approved GitHub Actions context proof from `build_ci_shadow_context_proof`, `predicted_blast_radius_paths`, `actual_sacrificial_delta_paths`, `sacrificial_delta_matches_prediction: true`, `executed: false`, and `mutated_live_repo: false`. Cover both dynamic Taskmaster cascade baseline branches under the exact same pinned Taskmaster `0.43.1` toolchain: one fixture where `.taskmaster/state.json` is absent before the sacrificial mutation and Taskmaster creates it, and one fixture where `.taskmaster/state.json` is already present before the sacrificial mutation and Taskmaster rewrites it. In both branches, `.taskmaster/state.json` is part of the validated dynamic blast radius and must appear in both `_predicted_paths` output and `actual_sacrificial_delta_paths`, alongside `.taskmaster/tasks/tasks.json` and the generated task markdown. Do not retain any wording or expectations that a pre-existing `.taskmaster/state.json` is unchanged or absent from the delta under the pinned `0.43.1` cascade. Add workflow contract tests in `tests/meta_workflow_guard/test_ci_workflows.py` and focused shadow tests in `tests/meta_workflow_guard/test_aegis_reconcile_shadow_apply.py` proving CI installs the deterministic CLI before pytest, uploads the bound evidence artifacts, records both conditional `.taskmaster/state.json` validations under the pinned toolchain, and exposes a reusable invalidation comparison contract for future apply code. Preserve the existing inertness guarantees: no `--apply`, no live `task-master set-status` against the governed repo, no MCP apply tool, no governed-repo mutation route, no Git/GitHub/closeout/session/workflow-state writes, and no persistent multi-run ledger. Update `docs/aegis/reconcile-shadow-apply-contract.md` or the adjacent reconcile promotion/apply contract documentation to state this is a CI pre-write evidence task only and to document the toolchain-binding rule: future apply evidence is valid only when it matches the same shared provisioning lock and runner/toolchain identity class, otherwise prior cascade evidence is stale and must be regenerated. The documentation must also state the corrected `state.json` behavior: for pinned Taskmaster `0.43.1`, both absent-before-mutation and already-present-before-mutation branches include `.taskmaster/state.json` in the validated sacrificial cascade delta because Taskmaster creates or rewrites that file. The only Taskmaster mutation remains restricted to detached sacrificial clones under temp directories, guarded by the existing temp-dir check in `_run_clone_command`. Do not introduce Task 153 behavior; leave any default-off write scaffold and partial-apply rollback criteria for the next task.

**Test Strategy:**

Run focused tests with a real pinned `task-master` CLI installed through the shared provisioning/lockfile path: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/meta_workflow_guard/test_aegis_reconcile_shadow_apply.py`. Verify the real sacrificial cascade tests execute rather than skip in the provisioned environment, including both `.taskmaster/state.json` baseline branches under the same pinned Taskmaster `0.43.1` toolchain: absent before mutation and pre-existing before mutation. In both cases assert `.taskmaster/state.json` appears in `predicted_blast_radius_paths` and `actual_sacrificial_delta_paths`, and that `sacrificial_delta_matches_prediction` is true. Run CI workflow contract tests: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest tests/meta_workflow_guard/test_ci_workflows.py tests/meta_workflow_guard/test_aegis_reconcile_shadow_apply.py -k 'ci or shadow or sacrificial or toolchain or taskmaster or state_json'`. Add or maintain tests that construct matching and mismatching toolchain evidence records and prove mismatches in Taskmaster version/source, provisioning lock identity, or relevant runner/toolchain fields invalidate prior cascade evidence before any future apply path may proceed. Run adjacent read-only/inertness guards from Tasks 144-151, including disabled apply scaffold, apply path proposal, mutation candidate preview, rollback contract, precision corpus, side-effect oracle, installer reconcile, and MCP server tests. Inspect generated `reports/ci/` artifacts locally or from GitHub Actions to confirm they include Taskmaster version/source, provisioning lock identity, runner/toolchain identity, approved CI context proof, predicted and actual sacrificial paths for both `.taskmaster/state.json` baseline states, `sacrificial_delta_matches_prediction: true`, `executed: false`, and `mutated_live_repo: false`. Confirm there is no assertion or documentation path expecting pre-existing `.taskmaster/state.json` to be unchanged or absent from the cascade delta under Taskmaster `0.43.1`. Finally confirm the workflow text and agent surfaces still contain no enabled apply route, no governed-repo Taskmaster mutation, no MCP apply route, and no persistent ledger.
