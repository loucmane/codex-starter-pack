# Task ID: 120

**Title:** Prove Fresh-Project Aegis Local Artifact Install and Claude Live-Test Rehearsal

**Status:** done

**Dependencies:** 119 ✓

**Priority:** medium

**Description:** Run a real-user-style pre-publication proof that Aegis can be built from this repository, registered from a local wheel or local package path, installed into a brand-new project outside the repo, and used through the Aegis workflow without PyPI/TestPyPI. Capture durable evidence and prepare a Claude live-test prompt that drives a small app change through install, kickoff, logging, verification, and closeout.

**Details:**

Codebase context: `pyproject.toml` defines the `aegis-foundation` Python package with console scripts `aegis = aegis_foundation.cli:main` and `aegis-mcp-server = aegis_mcp.server:main`, with packaged runtime assets under `aegis_foundation/assets/**`. `aegis_foundation/cli.py` delegates package-style commands to `scripts/_aegis_installer.py`, including `inspect`, `plan-install`, `install`, `status`, `verify`, `closeout`, `certify-release`, `kickoff`, `log`, and the `mcp generate-registration`, `mcp execute-registration`, and `mcp verify-registration` subcommands. `aegis_foundation/mcp_registration.py` is the native registration helper and already supports package, pinned, GitHub, local wheel, and local source modes that render `uvx --from <artifact-or-source> aegis-mcp-server --default-target-dir . --transport stdio`. `aegis_mcp/server.py` exposes the MCP surface through `aegis.inspect`, `aegis.status`, `aegis.plan_install`, `aegis.install`, `aegis.verify`, `aegis.closeout`, `aegis.kickoff`, `aegis.log`, `aegis.list_profiles`, and `aegis.explain_profile`, plus resources such as `aegis://manifest/current`, `aegis://work/current`, `aegis://verification/latest`, and `aegis://closeout/latest`. Existing regression coverage lives in `tests/meta_workflow_guard/test_aegis_release_distribution.py`, `tests/meta_workflow_guard/test_aegis_native_mcp_registration.py`, `tests/meta_workflow_guard/test_aegis_mcp_e2e_targets.py`, `tests/meta_workflow_guard/test_aegis_installer.py`, and `tests/meta_workflow_guard/test_aegis_cross_project_smoke.py`; this task should use those patterns but capture a fresh, human-readable Task 120 proof rather than relying only on prior automated smoke results. Implementation approach: 1. Kick off Task 120 with normal session, plan, and work-tracking surfaces. Create a Task 120 evidence area under the active work-tracking reports folder, for example `docs/ai/work-tracking/active/<task120-folder>/reports/fresh-local-artifact-install/`, and record the no-publishing boundary: no TestPyPI, no PyPI, no `twine upload`, no registry install of `aegis-foundation`; only local wheel or local source path may be used. 2. Build local artifacts from this repository using the existing release path where practical: `aegis certify-release --source-dir . --dist-dir <task120-report-dir>/dist --report-file <task120-report-dir>/certification-report.json`, or `uv build --sdist --wheel --out-dir <task120-report-dir>/dist` followed by the existing artifact inspection checks. Capture wheel and sdist paths, SHA-256 values, package version, Python version, git branch/commit, and required package members: console entry points, `aegis_foundation/cli.py`, `aegis_mcp/server.py`, `aegis_foundation/assets/scripts/_aegis_installer.py`, `aegis_foundation/assets/scripts/codex-task`, `aegis_foundation/assets/.claude/scripts/*`, `aegis_foundation/assets/schemas/aegis/*.json`, `aegis_foundation/assets/templates/aegis/workflow/*`, and packaged `docs/aegis/*`. 3. Create a genuinely fresh target project outside `/home/loucmane/codex`, preferably under `/tmp/aegis-task120-fresh-project` or another clearly external path. Seed it as a small app a real user might have, not a copy of this repository: for example a minimal web app with `package.json`, `index.html`, and `src/main.ts`, or a minimal Python app with `pyproject.toml`, source, and one test. Initialize a git repository on `main`. Assert there is no `.aegis`, `.claude`, `.taskmaster`, `.serena`, `sessions`, `plans`, or `docs/ai/work-tracking` before install. 4. Configure or register Aegis from the local artifact exactly as a real user would. Use the package CLI or local wheel invocation to generate the native Claude command: `aegis mcp generate-registration --client claude --scope project --source-mode wheel --artifact <absolute-wheel-path> --target-dir <fresh-target>`. Capture the JSON payload, including `client_argv`, `mcp_server_argv`, `rendered_command`, `package_spec`, `source_mode`, environment, and safety notes. If the real `claude` CLI is available, execute `aegis mcp execute-registration --client claude --scope project --source-mode wheel --artifact <absolute-wheel-path> --target-dir <fresh-target> --cwd <fresh-target>` and then `aegis mcp verify-registration` with the same inputs. If the real client is unavailable, record the structured `missing_client` result as a finding and still run the generated MCP server command directly through stdio as proof of the artifact path; do not silently substitute manual `.mcp.json` edits as the happy path. 5. Verify installed-artifact MCP startup from the fresh target with no checkout leakage. Clear `AEGIS_SOURCE_ROOT`, avoid source-checkout `PYTHONPATH`, and run `uvx --from <absolute-wheel-path> aegis-mcp-server --default-target-dir <fresh-target> --describe-config`. The output must report `distribution_name: aegis-foundation`, `asset_origin: package`, and a packaged asset root, not `/home/loucmane/codex`. Then start stdio using the generated `mcp_server_argv` and verify tool, resource, and prompt discovery against the expected names from `aegis_mcp.server`. 6. Install Aegis into the fresh target through the local artifact path, using the MCP tools where possible and the installed `aegis` command or `.aegis/bin/aegis` only where that mirrors a real user fallback. Run `aegis.inspect`, `aegis.status`, `aegis.plan_install`, `aegis.install` with explicit apply semantics, and `aegis.verify` with report-write acknowledgement as appropriate. Confirm `.aegis/foundation-manifest.json`, `.aegis/contract.md`, `.aegis/reports/install-plan.json`, `.aegis/reports/install-report.json`, `.aegis/reports/verification-report.json`, `AGENTS.md`, `CLAUDE.md`, `.claude/settings.json`, `.claude/scripts/readiness.sh`, `.claude/scripts/pretooluse-gate.sh`, `.claude/scripts/posttooluse-tracking.sh`, `.claude/scripts/tracking-stop-gate.sh`, `.claude/scripts/gate_lib.py`, and `.aegis/bin/aegis` exist. 7. Prove readiness and kickoff behavior. Before kickoff, `bash .claude/scripts/readiness.sh --quick --root <fresh-target>` should block because there is no task-scoped current work; representative PreToolUse mutations should also block. Run `aegis.kickoff` or `.aegis/bin/aegis kickoff --target-dir . --task 1 --slug local-artifact-live-proof --title Local Artifact Live Proof --goal <goal>` from the fresh target. Confirm the kickoff creates `.aegis/state/current-work.json`, `sessions/current`, `plans/current`, and an active `docs/ai/work-tracking/active/<date>-task1-local-artifact-live-proof-ACTIVE/` folder with `TRACKER.md`, `FINDINGS.md`, `DECISIONS.md`, `IMPLEMENTATION.md`, `CHANGELOG.md`, `HANDOFF.md`, `reports/`, and `designs/`. After kickoff, readiness must print a READY result for task 1 and must not require `.taskmaster` or `.serena`. 8. Prove tracking gates in the installed fresh project. Run a representative allowed source edit through the Claude hook scripts: PreToolUse allows the first task-scoped mutation after readiness; PostToolUse records `.aegis/state/pending-tracking.json`; a second mutation and Stop gate are blocked until `aegis log` records the pending evidence; `aegis log --handler <handler> --evidence <path> --note <past-tense note> --plan-step plan-step-implement --plan-status completed` clears the pending event and writes S:W:H:E tokens into the current session, active tracker, implementation log, changelog, handoff, and plan. Also verify protected Aegis-owned paths such as `CODEX.md`, `CLAUDE.md`, `.aegis/foundation-manifest.json`, and hook scripts are blocked from direct edits by installed gates. 9. Run strict verification and closeout from the fresh target. Use `.aegis/bin/aegis verify --target-dir . --strict`, log verification evidence with `aegis log`, then run `.aegis/bin/aegis closeout --target-dir . --update-handoff`. Capture the verification and closeout JSON reports and ensure required checks pass or, if they do not, record the exact gap in `FINDINGS.md` and the Task 120 report before any publishing task proceeds. 10. Add a source-checkout leakage scan over the fresh target and Task 120 evidence. Search target runtime state, reports, generated contracts, session, plan, and work-tracking files for `/home/loucmane/codex`, `AEGIS_SOURCE_ROOT`, source-checkout `PYTHONPATH`, and registry install commands. The only acceptable source-repo path references are Task 120 evidence metadata in this repository; installed target runtime should rely on the local wheel or packaged assets only. 11. Prepare a Claude live-test prompt as a committed evidence artifact, for example `<task120-report-dir>/claude-live-test-prompt.md`. The prompt should instruct Claude, when opened in the fresh target with the local-wheel Aegis MCP registered, to use discovered Aegis tools or `.aegis/bin/aegis` to inspect status, confirm readiness, perform one small app change such as adding a visible status/footer/button or a tiny health endpoint, record scope and implementation evidence through `aegis.log`, run an app-level verification command, run Aegis strict verify and closeout, and report only after the Aegis closeout passes. The prompt must explicitly say not to edit `.aegis/`, `.claude/`, `CLAUDE.md`, or `AGENTS.md` directly and to document any missing MCP tools or gate failures as findings. 12. Summarize findings in the active Task 120 `FINDINGS.md` and handoff. The final finding must state whether fresh-project local artifact installation is publishing-ready, publishing-blocked, or partially proven, and list any concrete gaps before TestPyPI/PyPI can begin.

**Test Strategy:**

Required verification: 1. Run the existing focused Aegis regression slice after any changes: `python -m pytest tests/meta_workflow_guard/test_aegis_release_distribution.py tests/meta_workflow_guard/test_aegis_native_mcp_registration.py tests/meta_workflow_guard/test_aegis_mcp_e2e_targets.py tests/meta_workflow_guard/test_aegis_installer.py tests/meta_workflow_guard/test_aegis_cross_project_smoke.py`. Include the invocation-contract tests if documentation or command wording changes: `python -m pytest tests/meta_workflow_guard/test_aegis_invocation_contract.py`. 2. Run the environment-gated local artifact smokes where the machine has `uv` and `uvx`: `AEGIS_RUN_WHEEL_SMOKE=1 python -m pytest tests/meta_workflow_guard/test_aegis_release_distribution.py::test_local_wheel_cli_smoke_when_enabled`; `AEGIS_RUN_WHEEL_MCP_SMOKE=1 python -m pytest tests/meta_workflow_guard/test_aegis_release_distribution.py::test_local_wheel_mcp_stdio_smoke_when_enabled`; `AEGIS_RUN_WHEEL_MCP_TARGET_SMOKE=1 python -m pytest tests/meta_workflow_guard/test_aegis_mcp_e2e_targets.py::test_local_wheel_mcp_real_target_project_smoke_when_enabled`; and, if present in the current suite, the full release certification smoke such as `AEGIS_RUN_CERTIFICATION_SMOKE=1 python -m pytest tests/meta_workflow_guard/test_aegis_installer.py::test_release_certification_full_clean_smoke_when_enabled`. 3. Verify the Task 120 manual fresh-project proof with captured command transcripts or JSON reports under work-tracking: local wheel/sdist build succeeds; checksums and required package members are recorded; native registration JSON uses `source_mode: wheel` or explicit local source mode; generated MCP command starts with `uvx --from <absolute-local-artifact-or-source-path>`; no command uses PyPI/TestPyPI, `twine upload`, `pip install aegis-foundation` from a registry, `test.pypi.org`, or `pypi.org` as part of the proof. 4. In the fresh target, assert the following states in order: initial project has none of the Aegis directories; `aegis-mcp-server --describe-config` from the wheel reports package assets; MCP stdio discovery includes the expected Aegis tools/resources/prompts; `aegis.inspect` reports not installed; `aegis.plan_install` is read-only; `aegis.install` refuses without apply and applies with apply; `aegis.verify` refuses without acknowledgement where required and passes when acknowledged; `aegis.status` reports current after install. 5. After kickoff, assert the generated files and scaffolding exist: `.aegis/foundation-manifest.json`, `.aegis/state/current-work.json`, `.aegis/bin/aegis`, `.aegis/reports/install-plan.json`, `.aegis/reports/install-report.json`, `.aegis/reports/verification-report.json`, `.aegis/contract.md`, `AGENTS.md`, `CLAUDE.md`, `.claude/settings.json`, `.claude/scripts/readiness.sh`, `.claude/scripts/pretooluse-gate.sh`, `.claude/scripts/posttooluse-tracking.sh`, `.claude/scripts/tracking-stop-gate.sh`, `sessions/current`, `plans/current`, and the active work-tracking files `TRACKER.md`, `FINDINGS.md`, `DECISIONS.md`, `IMPLEMENTATION.md`, `CHANGELOG.md`, and `HANDOFF.md`. 6. Exercise installed gates directly: readiness blocks before kickoff and returns READY after kickoff; PreToolUse blocks mutations while readiness is blocked; PreToolUse allows a valid task source edit after kickoff; PostToolUse creates pending tracking; a second mutation and Stop gate are blocked while pending tracking exists; `aegis log` clears pending tracking and updates S:W:H:E evidence in all required surfaces; protected Aegis-owned paths are refused; strict verify and closeout pass only after required evidence exists. 7. Scan the fresh target for source-checkout leakage: `rg -n '/home/loucmane/codex|AEGIS_SOURCE_ROOT|PYTHONPATH=.*codex|test.pypi.org|pypi.org' <fresh-target>`. Treat any runtime-state hit as a blocker unless it is explicitly part of a captured command transcript stored in the Task 120 evidence folder outside the target. 8. Review the Claude live-test prompt artifact manually against the fresh target acceptance: it must tell Claude to use Aegis MCP or `.aegis/bin/aegis`, perform one small app change, log scope/implementation/verification evidence, run app verification, run `aegis verify --strict`, run `aegis closeout --update-handoff`, avoid direct Aegis-owned file edits, and document any gaps. 9. Run repository workflow gates before marking the task complete: `python3 scripts/codex-task taskmaster health`, `python3 scripts/codex-task plan sync`, `python3 scripts/codex-task work-tracking audit`, `python3 scripts/codex-guard validate --include-untracked`, and `git diff --check`. 10. Acceptance is met only if the proof evidence shows no PyPI/TestPyPI usage, the fresh target was outside the repository, the local artifact/source path drove MCP registration or startup, installed Aegis assets and hooks were created, readiness and tracking gates behaved as designed, evidence was captured under Task 120 work-tracking, and `FINDINGS.md` clearly states any remaining gap before public publishing.
