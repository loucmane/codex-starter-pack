# Task ID: 228

**Title:** Dogfood Capsule Boundary Refresh Across Codex And HP-Coach

**Status:** done

**Dependencies:** 226 ✓

**Priority:** medium

**Description:** Run acceptance dogfood for the shipped capsule freshness and boundary-trigger model across Codex and downstream HP-Coach, verifying that one canonical capsule refreshes at the operator boundaries that matter. Record findings first and patch only concrete trigger-wiring bugs discovered during the dogfood pass.

**Details:**

Codebase analysis found the current capsule trigger implementation in `aegis_foundation/cli.py`: `BRIEF_REASON_CHOICES`, `_refresh_capsule_if_stale()`, `handle_next()` reason `orientation`, `handle_witness()` reason `pre-delivery`, `handle_verify()` reason `verification`, and `handle_brief()` for explicit `aegis brief --reason/--status/--check`. Boundary event recording is covered by `.claude/scripts/ledger_lib.py` and `aegis_foundation/assets/.claude/scripts/ledger_lib.py`; focused tests already exist in `tests/claude_adapter/test_capsule_boundary_triggers.py`, `tests/claude_adapter/test_ledger_record.py::test_recorder_refreshes_capsule_for_boundary_events`, `tests/claude_adapter/test_brief_lib.py`, and `tests/claude_adapter/test_capsule_injection.py`. The normative behavior is documented in `docs/aegis/AEGIS_CAPSULE_SPEC.md` section 3, which defines compile reasons `session-start`, `session-resume`, `post-merge`, `task-status-change`, `orientation`, `pre-delivery`, `verification`, `risk-register-change`, and `manual`, all writing `.aegis/capsule/current.md` and `.aegis/capsule/current.json`; the 2026-07-03 decision record makes resume-drift refresh the primary dogfood gate.

Treat this as an acceptance/evidence task, not a new architecture task. Start by creating normal Task 228 work-tracking artifacts and a concise acceptance matrix covering: Codex orientation via `aegis next`, Codex verification via `aegis verify`, Codex pre-delivery via `aegis witness`, task-status-change via `task-master set-status` or a controlled `tasks.json` write, post-merge via an actual or safely simulated merge boundary, SessionStart/resume/compact injection where available, and downstream HP-Coach runtime visibility after its installed Aegis runtime is refreshed. For each boundary, capture before/after `aegis brief --status --json`, `.aegis/capsule/current.json` `capsule_meta.compile_reason`, freshness snapshot changes, relevant ledger events, and whether the capsule stayed canonical rather than creating parallel state.

Use existing shipped surfaces first: package CLI `python -m aegis_foundation.cli ...`, local wrapper `python3 scripts/codex-task aegis ...`, installed `./.aegis/bin/aegis ...` in target repos, SessionStart hook routing through `.claude/scripts/gate_lib.py sessionstart`, and the recorder path that classifies post-merge/task-truth/risk-register-change events. For HP-Coach, verify the installed runtime can see the same boundary outcomes after `aegis plan-install` / `aegis install --apply` or the established runtime refresh path; capture installed asset versions/hashes and at least one real downstream capsule status/readout. Do not add new capsule fields, new trigger classes, narration work, retirement work, new MCP behavior, or unrelated witness features. If a boundary fails because an existing trigger is incorrectly wired, apply the smallest concrete patch at the current ownership point, usually `aegis_foundation/cli.py`, `.claude/scripts/brief_lib.py`, `.claude/scripts/ledger_lib.py`, `aegis_foundation/assets/.claude/scripts/*`, installer asset rendering in `scripts/_aegis_installer.py`, or the corresponding focused test. Keep live and packaged asset copies in parity where this repo already requires parity.

**Test Strategy:**

Run the existing focused capsule suite first: `python -m pytest tests/claude_adapter/test_capsule_boundary_triggers.py tests/claude_adapter/test_ledger_record.py tests/claude_adapter/test_brief_lib.py tests/claude_adapter/test_capsule_injection.py`. Then run acceptance dogfood steps against Codex and HP-Coach: for each boundary, record `aegis brief --status --json` before, trigger exactly one operator boundary, inspect `.aegis/capsule/current.json`, and confirm the expected `compile_reason` (`orientation`, `verification`, `pre-delivery`, `task-status-change`, `post-merge`, `session-start`/`session-resume` as applicable) plus fresh status after the trigger. Include `aegis brief --check` after successful refreshes to verify budget/canary/parse constraints still pass.

Acceptance requires evidence that `aegis next`, `aegis verify`, and `aegis witness` refresh only at boundaries and do not reintroduce per-mutation ceremony; task-status and post-merge recorder events refresh the canonical capsule; stale status is loud when HEAD, task truth, gate decisions, or ledger markers move; and HP-Coach sees the refreshed capsule through its installed runtime without hand-editing `.aegis/capsule/current.*`. If code is patched, add or update focused regression tests for the failing trigger and rerun the capsule suite plus `python3 scripts/codex-task taskmaster health`, `python3 scripts/codex-task work-tracking audit`, `python3 scripts/codex-guard validate --include-untracked`, and `git diff --check`. Record failures as findings when they are environmental or downstream install issues; only patch confirmed product trigger bugs.
