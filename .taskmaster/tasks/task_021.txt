# Task ID: 21
# Title: Implement Template Frontmatter Schema
# Status: done
# Dependencies: 6
# Priority: high
# Description: Create and enforce standardized frontmatter for all templates
# Details:
Define YAML schema with required fields: id, type (trigger|operator|orchestrator|guide), category, dependencies[], triggers[], exports[], version (semver), status (stable|beta|deprecated), tags[]. Create JSON Schema validator. Implement frontmatter parser using python-frontmatter. Add migration tool for existing templates. Create frontmatter generator CLI command.

# Test Strategy:
Validate schema against sample templates, test parser with various formats, verify migration tool preserves content, test validation errors

# Subtasks:
## 1. Scope reconciliation against portable foundation

### Status: done
### Dependencies: None

Before implementation, audit this task against templates/engine/core/portable-foundation-spec.md, the Task 1 codebase analysis, and the Task 4 backlog-alignment audit. Treat original details as historical context until current repository evidence confirms the remaining gap.

## 2. Implement proven current-state gap with evidence

### Status: done
### Dependencies: None

After the scope gate, update or expand this task with current implementation subtasks and capture guard, test, work-tracking, and Taskmaster evidence. Do not execute stale migration wording or unrelated generated subtasks.
