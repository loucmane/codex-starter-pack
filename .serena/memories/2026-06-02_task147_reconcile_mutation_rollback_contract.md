Task 147 defines a report-only rollback/blast-radius proposal contract for future Aegis reconcile mutation. Scope remains non-mutating: no changes to scripts/_aegis_installer.py::reconcile, scripts/codex-task aegis reconcile, aegis_foundation/cli.py::handle_reconcile, or MCP aegis.reconcile. Added tests/meta_workflow_guard/reconcile_mutation_rollback_contract.py and test_aegis_reconcile_mutation_rollback_contract.py. First candidate is only merged_but_not_done with git_ancestor proof; github_pr_merged, done_but_not_merged, manual-only findings, and squash/offline unknown truth are rejected. Proposal requires operator confirmation plus before/after audit breadcrumbs. Real isolated Taskmaster cascade uses task-master set-status --id=42 --status=done + task-master generate; exact changed paths are .taskmaster/tasks/tasks.json and .taskmaster/tasks/task_042.md. .taskmaster/state.json exists but is unchanged by the done cascade. Rollback handle restores registered paths and verifies the whole isolated tree. Docs added/updated: docs/aegis/reconcile-mutation-rollback-contract.md, reconcile-promotion-contract.md, reconcile-precision-corpus.md. Verification so far: focused tests 19 passed; combined reconcile contract suite 52 passed, 94 deselected.