# Task 131 PR #128 CI fix

- Date: 2026-05-31
- Context: PR #128 Python 3.11/3.12 CI failed because broader MCP smoke/e2e tests still expected `aegis.install` to return `ok: true` after the Task 131 hard-stop change.
- Fix: Updated `tests/meta_workflow_guard/test_aegis_cross_project_smoke.py` and `tests/meta_workflow_guard/test_aegis_mcp_e2e_targets.py` to assert `client_reload_required`, use the embedded install report, and simulate a restarted Claude PreToolUse hook before continuing verify/kickoff/workflow assertions.
- Verification: failing modules pass locally (`26 passed, 1 skipped`); full pytest passes locally (`793 passed, 4 skipped`).
