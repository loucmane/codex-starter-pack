Task 163 started after PR #162 merged. Implemented the minimal GitHub Actions Node 24 JavaScript-action runtime transition by adding top-level `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` to `.github/workflows/ci.yml`, `.github/workflows/codex-guard.yml`, and `.github/workflows/meta-workflow-guard.yml`. Preserved action major versions (`checkout@v4`, `setup-node@v4`, `setup-python@v4/v5`, `upload-artifact@v4`) and kept Taskmaster CLI on `node-version: "22"`. Added workflow contract coverage in `tests/meta_workflow_guard/test_ci_workflows.py` to require the Node 24 runtime opt-in, prevent accidental `upload-artifact@v5` migration in this task, and pin Taskmaster Node 22. Focused validation passed: `PYTHONDONTWRITEBYTECODE=1 uv run python -m pytest tests/meta_workflow_guard/test_ci_workflows.py tests/meta_workflow_guard/test_aegis_reconcile_shadow_apply.py::test_ci_workflow_captures_shadow_context_artifact_without_apply_surface -q` -> 11 passed in 0.40s. Final Task 163 verification remains open until a PR CI run confirms artifacts still upload with expected layout under the Node 24 runtime.
