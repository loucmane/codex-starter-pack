---
description: Run a codex-task scanner and store evidence.
allowed-tools: Bash
argument-hint: <tool> [options]
---

Run:

```bash
python3 scripts/codex-task scanner run $ARGUMENTS
```

If the scanner name is missing, run `python3 scripts/codex-task scanner run --help` and ask which scanner to run.
