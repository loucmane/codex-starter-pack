#!/usr/bin/env python3
"""Record strict local evidence for a completed Codex Desktop transport retest."""

import sys
from pathlib import Path


REPO_ROOT = Path(__file__).resolve().parent.parent
if str(REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(REPO_ROOT))

from aegis_foundation.reboot_readiness import retest_main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(retest_main())
