# Fable adapter

Use the same `gas-city-workflow.project-context.v1` capsule and project-local `CLAUDE.md` / `AGENTS.md`. Fable is an independent read-only reviewer by default: inspect evidence, challenge assumptions, and return PASS/HOLD. Do not treat caller identity, client membership, or a bead status as execution authority.
