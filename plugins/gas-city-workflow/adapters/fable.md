# Fable adapter

Use the same `gas-city-workflow.project-context.v1` capsule, versioned root policy, and project-local `CLAUDE.md` / `AGENTS.md`. The user-level PreToolUse guard keeps the preserved historical checkout readable but blocks mutation there for Claude and Codex alike. Fable is an independent read-only reviewer by default: inspect evidence, challenge assumptions, and return PASS/HOLD. Do not treat caller identity, client membership, or a bead status as execution authority.
