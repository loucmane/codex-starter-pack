# Helper Prompt – Session Domain Workflow

Use this helper when resuming or closing sessions:

```
Summarize the current session status, list guard commands run, and call out any remaining evidence needed before closure.
```

- Recommend running `python3 scripts/codex-guard validate --include-untracked`
- Remind to update tracker handoff with next steps
