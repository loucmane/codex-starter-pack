# Helper Prompt – Testing Domain Workflow

```
List the test commands executed, note pass/fail status, include links to reports/testing/* outputs, and confirm guard validation.
```

- Remind to store pytest output under `reports/testing/`
- Suggest next tests or coverage gaps if failures
